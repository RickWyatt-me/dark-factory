# Prod DB apply — {{PROJECT_NAME}}

<!-- TEMPLATE STUB. Only relevant if the OPTIONAL §2.4a road is enabled — FACTORY.md's
     "Migration laps" section points an operator here when a range parks. If the
     road is not enabled, prod apply is permanently human and this file can say
     just that plus your hand-apply procedure. -->

## The road (if §2.4a is enabled)

<!-- FILL: the apply endpoint `factory/db-apply.sh` uses, how it records applies
     so hand applies and factory applies are indistinguishable, where evidence
     lands (`.factory/deploy/db-apply/`), and the credential (name + storage +
     expiry — never the value). -->

## The three rungs

<!-- FILL: what the static allowlist (`factory/db_apply_policy.py`) passes in
     THIS repo; what routes to the independent migration judge; the exact
     human-floor statement classes and schema names ({{HUMAN_FLOOR_SCHEMAS}}). -->

## When a range parks

<!-- FILL: the hand-apply procedure — apply mechanism, positive verification
     steps, then re-run `factory/deploy.sh` or let the next tick's catch-up
     retry. -->
